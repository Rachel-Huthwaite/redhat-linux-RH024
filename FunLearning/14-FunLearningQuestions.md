# 14 - Fun Learning Quiz: Using Image Mode with Bootc

Test your active recall on the concepts introduced in Lesson 14.

---

### Questions

1. **Defining `bootc`:** What is `bootc` and how does Image Mode differ from traditional package-by-package Linux OS management?
2. **Checking Image Status:** What command displays the active container image tag and boot state on a `bootc` system?
3. **OS Upgrades:** What command stages a new container image (e.g., `quay.io/name/bootc_httpd:2`) to update a `bootc` host?
4. **Applying Changes:** What action must be performed after running `bootc switch` to boot the system into the newly staged container image?
5. **Key Advantage:** What primary operational risk does Image Mode eliminate compared to traditional servers updated over time via `dnf`?